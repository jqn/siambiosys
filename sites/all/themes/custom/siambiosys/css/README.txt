This folder is where the output from the compiled SASS/SCSS files
should be generated.

"/style.css" should be the only css file. All overrides and edits are
compiled within the "/sass/_overrides.scss" file.